const initialState = {
  // starting player position on x and y axis 
  position: [0, 0],
}

const playerReducer = (state=initialState, action) => {
  switch(action.type) {
    default:
      return state
  }
}

export default playerReducer;